{
  "name": "deck-compare",
  "version": "1.0.0",
  "private": true,
  "description": "Deck Compare — MTG (Chrome extension). Unit tests for the site parsers (background + DOM via jsdom) and pure-logic modules. No network.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
